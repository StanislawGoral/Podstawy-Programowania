#include<stdio.h>
#include<stdlib.h>

float zmiana (float x)
{
	return atof(napis);
}

int main()
{
	float liczba;
	char napis[256];

	gets(napis);

	liczba = zmiana(napis);

	printf("%f", liczba);
	
	return 0;
	
}

